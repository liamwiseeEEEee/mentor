You are editing the file mentor.html in this dashboard project. It is currently
a working "Nova" AI Mentor page: a 3D avatar (left), a chat box (right), and a
"cozy loader" that shows while the mentor is thinking.

GOAL
Turn the mentor from a hardcoded macros/food coach into a GENERAL mentor that
answers using ALL of the user's dashboard data (food, water, gym, finance,
sleep/fitband, goals, peak/readiness, supplements — whatever the dashboard
stores). Do NOT change any design, layout, colors, avatar, animations, or CSS.
ONLY change (1) where the reply text comes from and (2) the cozy-loader sayings.

WHAT TO REMOVE
- The hardcoded `REPLIES` array and the `FALLBACK` array (the keyword-matched
  canned coach answers about protein/macros/etc.).
- The `pickReply()` keyword matcher.
- The food/macros-specific cozy-loader lines in `CZ_LINES` and tags in `CZ_TAGS`.
Leave EVERYTHING else (the Nova 3D engine, the chat bubbles, the cozy loader
component, the palette switcher, the avatar reactions) exactly as it is.

WHAT TO ADD — REAL AI REPLIES FROM DASHBOARD DATA
1. Read the user's dashboard data. The dashboard stores its data in the browser
   under localStorage (each app saves its own keys). Gather it like this:
       function readDashboardData() {
         const out = {};
         for (let i = 0; i < localStorage.length; i++) {
           const k = localStorage.key(i);
           // skip the API key and theme so we don't leak/clutter
           if (k === 'anthropic_key' || k === 'patron_theme') continue;
           try { out[k] = JSON.parse(localStorage.getItem(k)); }
           catch { out[k] = localStorage.getItem(k); }
         }
         return out;
       }
2. Replace the canned-reply logic in `respond()` so that, after the cozy loader
   shows, it calls Claude with the user's message + their dashboard data as
   context, and renders the returned text in the SAME coach bubble (keep the
   <span class="tagchip">Nova</span> and the fade reveal).
       async function askNova(userText) {
         const key = localStorage.getItem('anthropic_key');
         if (!key) return "Add your Anthropic API key in settings to chat with me.";
         const data = readDashboardData();
         const res = await fetch('https://api.anthropic.com/v1/messages', {
           method: 'POST',
           headers: {
             'x-api-key': key,
             'anthropic-version': '2023-06-01',
             'anthropic-dangerous-direct-browser-access': 'true',
             'content-type': 'application/json',
           },
           body: JSON.stringify({
             model: 'claude-opus-4-8',
             max_tokens: 1024,
             system:
               "You are Nova, the user's personal mentor inside their life-tracking " +
               "dashboard. You can see ALL their data (food, water, gym, finance, " +
               "sleep, readiness, goals, supplements). Use it to give honest, " +
               "specific, encouraging guidance across any area of their life — not " +
               "just nutrition. Keep replies short and always give ONE thing to act " +
               "on. Here is their current dashboard data as JSON:\n" +
               JSON.stringify(data),
             messages: [{ role: 'user', content: userText }],
           }),
         });
         const json = await res.json();
         if (json.error) return "Hmm, I couldn't reach my brain just now — check your API key.";
         return json.content[0].text;
       }
   Make the bubble that the cozy loader turns into show `await askNova(text)`.
   Keep the existing Nova facial reactions (surprised -> thinking -> a mood).
3. Update the cozy-loader sayings to be GENERAL mentor lines (not macros). Keep
   the same { text, hl } format and the same number of lines/tags:
       const CZ_TAGS = ['Thinking', 'One sec', 'On it'];
       const CZ_LINES = [
         { text: 'Reading your whole day across the dashboard.', hl: 'whole day' },
         { text: 'Finding the one move that matters most right now.', hl: 'one move' },
         { text: 'Keeping it honest and something you can do today.', hl: 'do today' },
       ];

4. Add a small settings control so the user can paste their Anthropic API key,
   saved to localStorage under 'anthropic_key'. Match the existing visual style
   (reuse the .starter / chip styling already in the file). Do NOT restyle the
   page — just a small "⚙ API key" input that fits the current look.

CONSTRAINTS
- Do not change any CSS, the avatar engine, the layout, or the cozy-loader visuals.
- Only the reply SOURCE and the cozy-loader TEXT change.
- Keep it a single self-contained mentor.html file.
